<Window x:Class="HarryManual.ItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HarryManual"
        mc:Ignorable="d"
        Title="ItemWindow" Height="300" Width="500">
    <Grid>
        <StackPanel>
            <ListView x:Name="ContentView">

            </ListView>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Button x:Name="Edit"  Grid.Column="0" Content="Редактировать"></Button>
                <Button x:Name="Delete" Grid.Column="1" Content="Удалить"></Button>
            </Grid>

            <Button x:Name="Export" Content="Экспорт в файл"></Button>
        </StackPanel>

    </Grid>
</Window>
